generated: '2026-07-20'
method: searched
source: >-
  openapi/process-street-openapi-original.json (ErrorCode enum + ErrorInfo
  schema) enriched from the API reference description.
format: custom-json-envelope
envelope:
  fields:
    error: Human-readable message. Do not parse programmatically.
    errorCode: Machine-parsable classification. Branch on this, not the message.
    requestId: Correlation id for support; present on handler-generated errors.
    details: >-
      Optional structured context. For Validation errors the keys are field
      paths; for PaymentRequired the keys describe the limit that was hit.
  note: >-
    errorCode and requestId are populated on responses generated by the public
    API exception handler (the great majority). A few low-level failures (JSON
    parse errors, framework routing errors) may return an ErrorInfo without
    them; in that case fall back to the HTTP status code, whose semantics match
    the corresponding errorCode.
error_codes:
- code: BadRequest
  http_status: 400
  meaning: The request was malformed or otherwise could not be processed.
- code: Unauthorized
  http_status: 401
  meaning: Missing or invalid API key.
- code: PaymentRequired
  http_status: 402
  meaning: A plan/billing limit was hit; details keys describe the limit.
- code: Forbidden
  http_status: 403
  meaning: Authenticated but the key's user lacks permission for the resource.
- code: NotFound
  http_status: 404
  meaning: The resource does not exist (or was already deleted).
- code: MethodNotAllowed
  http_status: 405
  meaning: HTTP method not supported on this resource.
- code: Conflict
  http_status: 409
  meaning: The request conflicts with the current state of the resource.
- code: PayloadTooLarge
  http_status: 413
  meaning: The request body exceeds the allowed size.
- code: Validation
  http_status: 422
  meaning: Request failed validation; details keys are the offending field paths.
- code: RateLimited
  http_status: 429
  meaning: Rate limit exceeded; wait for the Retry-After header duration.
- code: InternalError
  http_status: 500
  meaning: An unexpected server-side error; safe to retry idempotent requests.
